# Resources

This directory is reserved for pinned DSC resource dependencies that ship inside a reviewed ProStateKit bundle.

Production endpoint runs must not download resources at execution time. Add resources here only after the resource source, version, license, and hash are recorded in `bundle.manifest.json`.
