# DSC Runtime

Place the reviewed DSC executable for the target platform in this directory before building a release bundle.

The repository intentionally does not include a runtime binary yet. `Build-Bundle.ps1` and production runs fail closed until the pinned runtime version and hash are chosen intentionally.
